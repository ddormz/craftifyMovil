import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.craftifymovil',
  appName: 'Craftify N',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
