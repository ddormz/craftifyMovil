import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.craftifymovil',
  appName: 'craftifymovil',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
